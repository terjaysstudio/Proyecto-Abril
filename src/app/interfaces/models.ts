export interface Usuario {
  nombre: string;
  puntajeTotal: number;
}

export interface Senia {
  id: number;
  imagenUrl: string;
  opciones: string[];
  respuestaCorrecta: string;
  tipo: string;
}
